import Actions from '../components/Actions'
const chai = require('chai')
const expect = chai.expect

describe('Actions', () => {
  it('has a method \'takeAll\' that loops through the items in the world and returns a string to take them all', () => {
    const actions = new Actions()
  })
})
