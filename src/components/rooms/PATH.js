import {ROOM} from '../classes'

const PATH = new ROOM('path')
PATH.description =
    'You\'re on a long, winding path. To the south you see the cave entrance. To the north you see a small house.'

export default PATH
